{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions only. Shortcuts are registered from Rust at launch, not from the frontend, so the global-shortcut plugin needs no capability here — its own default permission set is empty by design, since the plugin treats shortcut registration as something an app should opt into explicitly.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
